


export const likeService = {
    async likeRestaurant(req) {
        return " like thanh cong"
    },
    async unlikeRestaurant(req) {
        return " like thanh cong"
    },
    async getLikesByRestaurant(req) {
        return "get likes by restaurant thanh cong"
    },
    async getLikesByUser(req) {
        return "get likes by user thanh cong"
    }
}

export default likeService;
